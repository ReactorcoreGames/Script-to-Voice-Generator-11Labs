"""
Theme and styling utilities for Script to Voice Generator GUI.
Custom theme removed — the app now uses ttkbootstrap's native 'flatly' theme.
This module is kept as a no-op stub so existing imports don't break.
"""


def apply_app_theme(root):
    """No-op — flatly theme is applied by ttkbootstrap at Window creation."""
    pass
